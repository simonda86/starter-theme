{% extends "base.twig" %}

{% block content %}
    <div class="container">
        <div class="row">
            <div class="col-sm-12">
                <h1>{{ title }}</h1>
                {{ content }}
            </div>
        </div>
    </div>
{% endblock %}
